- "our networking situation is signficationly simplied to the current real bitcoin in that we do not have pools. "


- "currently the wallet-node communcation goes like this:

a) wallet queires the node for the UTXOs that belong to it 
b) the node sends over the information 
c) the wallet assembles transactions and sends them to the node // transactions come directly from the wallet
d) the node verifies them and adds them to the mempool, sending confirmation on back to the wallet 

"

- we here take the simplest way we could implement networking, i.e. through TCP so that we didn't have to worry baout data integrity
- we also simplify by using the length-prefixed encoding (this means taht we will serialize each message into a binary fomrat, in our case CBOR since we are alreaedy using)

- our protocal is not necessarily backward and forward compatible --> for later (the improver reading this information) we need to make the protocol smarter in order to do that and introduce some versioning and version-negotiation mechanisms. a good tool for such functionality would be (for example) gRPC with Protocol Buffers, bu tt that is an additional complexity that we will not be initroducing in this book. However, if you want, this is a great improvement you can make to the project. 

- threads are expensive so here we do not use them; could use in the future if we are doing realtime computing

- we want a practical way to avoid spawning many threads --> this is where cocurrent programming comes to play.
- concurrent programming is a general term for an approach which allwos having more than one task in progress at once... the difference between conceurrent, parallel, and distrubuted programming is that in concurrent 
programming all taks can run on one thread in parallel programming, task run simultaneously, whereas dsitrbuted progrmmming uses multiple process, often each running on a separate machine...
- cocurrent = all processes run on a single thread... can use event driven programming as well (as an alternative to threads)
- rust has only one built-in method to due cocurrency, i.e. asynchronous programming

- a future is a promise that at some point in the future a value of a given type will be available
- rust does not come with any built in runtime, the most commonly used are tokio or 
- the crate bastion is what people usually use for facilitating distributed programming

-mxining async frameworks is generally very bad of an idea and very difficult to do practically


- we can use this in the wsl terminal to generate a pair of keys to place in the miner folder for testing
cargo run --bin key_gen ../miner/alice

- here we are creating a central Miner structure, which manages the entire state of the applicatoin ; in order to be effectieve we will neeed to mnie on a didfference thread, so that we do NOT block the tokio runtime. In order to be able to communicate with te hardware thread effectively, we need to use so-called "channels"

-we do not utilize the built in standard library mpsc (multiple producer Single Consumer) crate for channel in std bc it carries with it 2 caveats: (1) It cannot receive or send messages asynchronously, (2) it is not multiple consumer (mpmc)

-arc (and the fact that our sender struct is clonable), we can create copies that are not actually copies, but rather second handles into the samr piece of memory. Rust forces us to use these types through the liberal amoung of compile errors it provides if you try to do multi-threading without them


-Potential improvements to be made to the miner: multithreaded mining could be implemented, as we are only mining with a single CPU core at the moment; it would also be a good idea to be able to submit your block to multiple nodes.

-we have a constraint on the node-- the maximum amount of transctions allowed in a block. 

- LIBRARY DEFINITIONS:
1) anyhow - this library is often used together with thiserror. while thiserror provides  a macro to create a concreate type covering all sorts of errors, anyhow provides ad dynamic type (and a Result<T> alias  that is using this type), whic hcan be converted into
2) argh is just more lightweight version of clap,  whic is the CLI app framework for Rust, i.e. aides in communication across nodes
3) dashmap: provides a fast HashMap that is thread-safe and has interior mutability.
4)static_init - one of the many rust libraries for creating global variablesfor types that require allocations.

- the node implementation is incredibly simplistic

- the find_longest_chain method in the node crate has been brutally simplified and could potentially serve as a security problem/risk, as an atck might create a bogus malicious node with a very long blockchain and it would propagate from there (we need a consensus mechanism for this, should implement after).

- the "longest chain" is a simplification: what actually matters is the chain with the most cumulative proof-of-work (usually the same as the longest, but not always, since blocks can have different difficulty)

- for the download_blockchain method in the node.rs crate, we can improve by instead of making many small request, we could add another message type that would return an entire chain of blocks.

- our wallet here is done more in a similar way to how they are done in production. We will split our wallet into two discrete pairs:
1) a core that handles functionailty, and knows nothing about the UI of the application
2) the interface of the application itself

we want the core to run pretty much independetly from the UI with the UI making requests from the core of the application

-a core (wallet) that handles functionality, and knows nothing about the UI of the applicatoin
-the interfact of the application itself

- the second part is the TUI interface of the application itself that does not iteract with the wallet logic 

want the core to run pretty much independently from the UUI (wit the UI making requrest from the core of the eapplication and fetching info) 


core should NOT be a global variable because it is not necessary in rust 

core will be a structure that uses interior mutability so that it can be easily shareable via Arc<Core>

--> recall taht Arc<T> (Atomcic Reference Counting) is a thread-safe smart pointer in Rsust thatllow multiple ownsership of the same data acorss thread so we can avoid having to set things mut and can have multiple pointers "owning" the same actual value, not just a Reference --> the is the standard for cocurrent applications in rust 

cloning an Arc 

--> remember that CLI means jsut "command line interface"= a way for the user to interact with the program easily 

--> the fee configuration in the wallet could be more complex than the current implementation 

--> the transaction handling is simplified


-->> IMPORTANT: in handle_transactions, we are wiating to receive fully formed transactions (they will be coming frpm the UI). this solution will be a bit odd, as we will be taking a trip in and out of the core with the transactions (as the sender fro this receiver is loaced in the core)
-->Can we come back later refactor it so this does not happen?? important!!


--> the implementation of core could be significantly improved by not opening connections over and over again, but rather, storeing one connection in Core, and reusing it in both tasks (keep in you will have to lock in in a Mutex properly, to ensure one task doesn't start sending messages while the other is waiting for a reply)

-->a "challenge for us" presented in the editing of the core.rs crate in the TUI implementation was to: make the Key fields public, which we will need in a different part of the program